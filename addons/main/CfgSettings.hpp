
class CfgSettings {
    class CBA {
        class Versioning {
            class FISH_camo_cream {
                class dependencies {
                    //Addon will hard exit if this is missing
                    CBA[] = {"cba_main", REQUIRED_CBA_VERSION, "(true)"};
                };
            };
        };
    };
};
